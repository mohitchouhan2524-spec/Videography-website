# assets/icons/

Optional folder for any custom icon files (e.g. a favicon.ico for
older browsers, or PNG app icons for "Add to Home Screen").

The site currently uses inline SVG icons (no files needed) plus
assets/logo/aperture-mark.svg as the favicon, so this folder is
empty by default — add files here only if you want to customise
further.
